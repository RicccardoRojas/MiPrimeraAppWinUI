<UserControl
    x:Class="MiPrimeraAppWinUI.BotonProductos"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MiPrimeraAppWinUI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="LightBlue">
        <Button BorderThickness="0"
                Background="Transparent"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Stretch"
                Tag="{x:Bind TagID, Mode=OneWay}"
                Padding="10">

            <StackPanel Orientation="Vertical" VerticalAlignment="Center">
                <Image Source="{x:Bind RutaIcono, Mode=OneWay}"
                       Height="80" Stretch="Uniform"/>
                <TextBlock Text="{x:Bind Texto}" FontSize="20" FontWeight="Bold"
                           HorizontalAlignment="Center" TextWrapping="Wrap"/>
            </StackPanel>

        </Button>
    </Grid>
</UserControl>
